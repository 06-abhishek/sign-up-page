<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Account Created Successfully</title>
    <style>
      /* Modal styling for separate page */
      body {
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
        margin: 0;
        background-color: #f7f7f7;
        font-family: Arial, sans-serif;
      }

      .modal-content {
        background-color: #fff;
        padding: 20px 30px;
        border-radius: 10px;
        text-align: center;
        max-width: 400px;
        border: 2px solid rgba(255, 255, 255, 0.8);
        box-shadow: 0 0 30px rgba(150, 150, 150, 0.4);
      }

      .modal-content h2 {
        font-size: 24px;
        color: #4a4a4a;
        margin-bottom: 10px;
      }

      .modal-content p {
        color: #7a7a7a;
        font-size: 14px;
      }

      .modal-content button {
        background-color: #007bff;
        color: #fff;
        padding: 8px 16px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        transition: 0.3s ease;
      }
      .modal-content button:hover {
        background-color: #0056b3;
      }

      /* Responsive design (Mobiles) : */
      @media only screen and (max-width: 450px) {
        .modal-content {
          box-shadow: none;
          border: none;
          background-color: #f7f7f7;
        }
      }
      /* Responsive design (Tablets) : */
      @media only screen and (min-width: 451px) and (max-width: 768px) {
        .modal-content {
          max-width: 500px; /* Center and adjust size */
          box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
          border-radius: 10px; /* Soft rounding */
          padding: 20px; /* Spacing for tablets */
          margin: auto;
          background-color: #ffffff; /* Maintain consistent color */
        }
      }
    </style>
  </head>
  <body>
    <div class="modal-content">
      <h2>Account Created Successfully!</h2>
      <p>
        Your account has been created. You can now log in with your credentials.
      </p>
      <button onclick="location.href='/'">Go Back</button>
    </div>
  </body>
</html>
