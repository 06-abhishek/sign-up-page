<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Invalid Request</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f7f7f7;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
      }

      .container {
        text-align: center;
        padding: 20px;
        /* border: 2px solid #ff4c4c; */
        border-radius: 15px;
        background-color: white;
        box-shadow: 0 0 30px rgba(150, 150, 150, 0.4);
        /* box-shadow: 0 0 50px rgba(255, 0, 0, 0.2); */
      }

      h1 {
        color: #ff4c4c;
        margin-bottom: 20px;
      }

      p {
        color: #4a4a4a;
        margin-bottom: 20px;
      }

      button {
        padding: 10px 20px;
        background-color: #007bff;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 14px;
        transition: 0.3s ease;
      }

      button:hover {
        background-color: #0056b3;
      }

      /* Responsive design (Mobiles) : */
      @media only screen and (max-width: 450px) {
        .container {
          box-shadow: none;
          border: none;
          background-color: #f7f7f7;
        }
      }
      /* Responsive design (Tablets) : */
      @media only screen and (min-width: 451px) and (max-width: 768px) {
        .container {
          max-width: 500px;
          padding: 20px;
          box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
          border-radius: 10px;
          margin: auto;
          background-color: #ffffff;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Invalid Request</h1>
      <p>You have done invalid request or don't have access of reset link.</p>
      <button onclick="goBack()">Go Back to Login</button>
    </div>

    <script>
      function goBack() {
        // Redirect to the login page
        window.location.href = "/sign_in"; // Change this to your login page URL
      }
    </script>
  </body>
</html>
